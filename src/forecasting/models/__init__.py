"""Candidate price-forecasting models compared in Phase 2.

Every model implements the ``Forecaster`` protocol and sees only information
sets, so the walk-forward harness keeps each of them leak-free.
"""
